#!/bin/bash
VERSION="${BITBUCKET_BUILD_NUMBER:=0}.$(echo ${BITBUCKET_COMMIT:=latest}| cut -c1-6)"
GIT_BRANCH=${BITBUCKET_BRANCH:=""}
GIT_COMMIT=${BITBUCKET_COMMIT:=""}
GIT_OWNER=${BITBUCKET_REPO_OWNER:=""}
GIT_SLUG=${BITBUCKET_REPO_SLUG:=""}
GIT_DOMAIN="bitbucket.org"

